# 验证记录

[English](VALIDATION.md) · 简体中文 · [返回首页](README.zh-CN.md)

本文记录 0.2.0 版本的实际证据，并单独标注此前的浏览器检查及 0.1.1 结果；不表示完整 Office 兼容性保证。

## 0.2.0 P0/P1 复查 — 2026-09-26

围绕多语言、导入诊断和演示页持久化，确认并修复三项 P1 问题；本轮检查路径中未发现 P0。

- **诊断匹配阻塞主线程：**PPTX 图片引用中的重复分隔符会使翻译正则反复回溯。修复前，25,008 字符的消息超过子进程三秒期限；修复后单次约 0.34 毫秒，250,008 字符样本约 1.36 毫秒。这是隔离解析样例，不代表通用浏览器性能保证。现有诊断测试增加了带超时的子进程，检查五种语言下的长引用和未知消息。
- **恢复失败覆盖原存档：**Chrome 复现了不支持的已保存文稿在页面离开后被示例文稿替换。修复后，在回退页面继续编辑并离开，原记录仍保留，自动保存保持暂停。
- **旧标签页覆盖新内容：**两个存储连接及两个真实演示标签页均复现了覆盖。新版标签页现在在写入事务中检查版本；Chrome 验证旧页面编辑、离开后，较新的文稿仍保留，日文和韩文冲突提示正常。同一实例排队写入仍保持顺序。
- 草稿输入也触发自动保存。Chrome 使用模拟组合事件验证了无已提交 `change` 事件时保存草稿、导航后恢复，以及仅在未保存时取消 `beforeunload`。这是实际浏览器中的处理器检查，不等同于逐个操作系统输入法及原生离开对话框的人工验收；强制结束进程仍可能丢失未保存内容。
- OrbStack Node 24 中 **41 项测试通过**，ESM／IIFE、静态演示构建、离线安装包和严格消费端类型检查通过。本机 Chrome 验证两种产物的五语言切换、文稿／选区／历史隔离、只读及销毁重挂；均成功导入含 25,000 字符图片引用的合成 PPTX，切换语言后继续编辑、撤销重做和导出。检查页面无相关控制台错误或警告。
- 当日 `npm audit` 为 **0 项已知漏洞**。OrbStack 审计请求因证书链校验失败，随后使用原生 Node 和系统信任库完成，未关闭 TLS 检查。本轮未重新进行 WPS／PowerPoint 或其他浏览器原生验收。

## 0.2.0 多语言功能验证 — 2026-09-25

- OrbStack Node 24 中 **41 项测试通过**，新增检查覆盖本地化默认内容、文稿／历史隔离、组合诊断翻译及文件名保留。ESM／IIFE、静态演示、离线安装包和严格消费端类型检查通过。
- 本机 Chrome 中逐一切换 `zh-CN`、`zh-TW`、`en`、`ko`、`ja`，幻灯片内容保持原样；目视检查英文、韩文、日文布局和 450px 窄容器侧栏。
- 构建后的 ESM：输入中／英／日／韩文字、设置粗体，切到韩文后撤销重做正常，正在编辑的文字和样式保持可用，另一日文实例未变化。
- 构建后的 ESM／IIFE API 检查通过：语言及 DOM 属性、独立实例、不触发文稿变更、无效语言原子拒绝、模拟组合输入期间保留文字节点和选区、标题输入保留、历史、只读、隐藏工具栏及销毁重挂。模拟组合事件不等于逐个操作系统输入法的人工验收。
- 构建后的 IIFE：打开带兼容性说明的合成 PPTX，说明可随五种语言更新；加密 PDF 的密码输入和错误提示在日／韩文间切换，随后成功导入并完成韩文放映和退出。关闭对话框后控件可继续使用，未观察到相关控制台错误或警告。
- React／Vue 示例已增加响应式 `locale` 属性，未单独运行 React／Vue 应用。浏览器原生文件选择器仍使用系统／浏览器语言；本次界面功能未重新进行 WPS／PowerPoint 原生验证。

## 自动检查 — 2026-09-25

环境：OrbStack 中的 Node.js 24（`node:24-bookworm`）。

- **39 项测试通过：**JSON 快照隔离与往返、图片恢复／清理、稳定 ID、无效输入原子拒绝、历史合并、几何坐标、PDF 限制、PPTX 解析及原生可编辑导出。
- 优化回归检查在提交后修改宿主补丁和剪贴板元数据，验证独立的撤销／重做快照，并覆盖资源 ID 为 `toString` 的图片复制。
- 回归覆盖损坏／SVG-only 图片引用、透明度、渐变、阴影、隐藏对象／页面、折线连接符、换行、粘贴富文本和失败草稿恢复。
- 发布复查新增：同步／异步宿主回调异常隔离、PPTX 非法 XML 字符、文稿重载期间插入图片，以及文字编辑／只读模式中的保存快捷键。
- 依赖审计当时为 **0 项已知漏洞**。首次容器审计遇到本机证书链问题，随后使用原生 Node 和系统信任库完成，未关闭 TLS 校验。漏洞库结果可能变化。

命令：`npm test`、`npm run build:demo`、`npm run check:package`。CI 在 Node 24 上重复测试、构建和安装包 TypeScript 接入检查；发布提交的结果见 [CI](https://github.com/SamuelSupe/slidekit/actions/workflows/ci.yml)。

## 性能样例 — 2026-09-25

环境为 OrbStack 中的 Node.js 24.19.0、Linux arm64。合成文稿包含 100 页、每页 20 个元素，每次修改一个元素的位置。前后实现在七轮中交替运行，每轮预热八次并计时 30 次修改，核心提交耗时中位数由 **8.69 ms 降至 5.09 ms，约减少 41%**。测量仅包含文稿数据处理，不含 DOM 渲染或导入导出，不代表端到端延迟或最大容量保证。

## 浏览器证据

使用本机桌面 Chrome，没有引入新的浏览器自动化框架。通过本地 HTTP 服务验证演示页与构建后的 ESM／IIFE 示例。README 使用编辑器的实际截图。

此前已完成多页编辑、中文文字、局部格式、多选排版、缩放、放映、JSON 下载与恢复、PPTX 导入导出、双实例隔离、只读和销毁重挂。PDF 验证覆盖加密／取消导入，以及一个 171.72 MiB、12 页的合成图片 PDF；这是单次实测样例，不代表通用容量保证。

上一轮在构建后的 ESM 和 IIFE 接入页验证了隐藏页跳过、独立透明度、不可见线条、编号列表粘贴恢复与放映控制后的键盘焦点，并检查了实际下载的 JSON/PPTX。

### 0.1.1 优化检查 — 2026-09-25

- 构建后的 ESM：中文文本和粗体、连续撤销重做、插入图片、共享图片的多选复制及跨实例粘贴。所有粘贴图片均完成解码；第二实例在被明确操作前保持不变。
- 删除全部十个对象后撤销，四个图片对象全部恢复；加载快照、销毁重挂后保留全部十个对象，第二实例的七个对象也保持完整。
- 构建后的 IIFE：新增页面、撤销重做和销毁重挂保留预期页数，第二实例保持一页、两个对象。
- 两种示例均在 1512×861 的本机 Chrome 中验证，没有相关控制台错误或警告；版本号及文档调整不改变这些流程。

### 0.1.0 发布检查 — 2026-09-25

- 构建后的 **ESM 与 IIFE**：注入同步／异步宿主保存异常，其他订阅和撤销重做仍正常；同 ID 文稿重载后拒绝陈旧图片插入。含控制字符的文字可导出并重新打开，保留合法中文与 emoji，原 JSON 不变。销毁重挂及第二个只读实例互不影响。
- 实际在**文字编辑中按 ⌘S**，下载 JSON 包含当前中文／英文草稿。重新打开文件恢复全部 3 个对象，第二实例保持 2 个对象、0 次变更。
- 通过实际文件选择器加载 JSON，并在受控的读取边界模拟宿主继续编辑：导入拒绝替换，保留新文字。延迟读取钩子仅位于被排除的 QA 页面。
- 最终静态 ESM／IIFE 检查无相关控制台错误或警告。首次检查曾命中开发服务器缓存的旧 `dist` 模块，改为静态发布服务后重新验证通过。
- 使用独立 XML 解析器检查实际下载的 PPTX，全部 XML 和关系文件可解析。
- tarball 在全新消费项目中离线安装，公开导出的严格 TypeScript 检查通过，覆盖隐藏页、渐变、透明度、导入和生命周期。包中包含双语文档、依赖许可与真实截图，排除 QA 样例。

地址：`http://127.0.0.1:5183/examples/esm.html`、`/examples/iife.html`，以及加载同一构建产物的合成 QA 页面。桌面视口为 1512×861；README 截图来自本机 Chrome 的 5182 端口演示页。

## Office 与未覆盖范围

- **当前版本未完成 WPS／PowerPoint 原生打开和编辑验收。**此前 WPS 操作被无法访问的原生对话框阻断；浏览器渲染与 OOXML 检查不能替代 Office 验收。
- 未穷尽验证其他桌面浏览器、手机触控、任意第三方 PPTX 或最大容量文稿。
- PDF 内容会图像化，PPTX 仅支持文档所列子集。字体度量、复杂效果、布局继承及不支持对象可能影响结果，见[兼容性说明](docs/guide.zh-CN.md#pptx-打开)。
- IndexedDB 仅用于演示便利，不等于备份或多标签协作机制；宿主需要处理持久化失败。

## 本地复核

```sh
npm ci
npm test
npm run build:demo
npm run check:package
npm run preview
```

在 Chrome 打开 `http://localhost:5183`、`/examples/esm.html` 和 `/examples/iife.html`。实际操作变更流程、检查控制台、保存并重新加载文稿，核对另一个实例不受影响。在宣称具体 Office 产品兼容前，需要手工打开和修改文件。请使用合成样例；个人验收文稿与临时产物不进入 Git 或发布包。
