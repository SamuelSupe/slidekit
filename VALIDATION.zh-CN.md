# 验证记录

[English](VALIDATION.md) · 简体中文 · [返回首页](README.zh-CN.md)

本文记录 0.1.0 发布版本的实际证据，不表示完整 Office 兼容性保证。

## 自动检查 — 2026-09-25

环境：OrbStack 中的 Node.js 24（`node:24-bookworm`）。

- **38 项测试通过：**JSON 快照隔离与往返、图片恢复／清理、稳定 ID、无效输入原子拒绝、历史合并、几何坐标、PDF 限制、PPTX 解析及原生可编辑导出。
- 回归覆盖损坏／SVG-only 图片引用、透明度、渐变、阴影、隐藏对象／页面、折线连接符、换行、粘贴富文本和失败草稿恢复。
- 发布复查新增：同步／异步宿主回调异常隔离、PPTX 非法 XML 字符、文稿重载期间插入图片，以及文字编辑／只读模式中的保存快捷键。
- 依赖审计当时为 **0 项已知漏洞**。首次容器审计遇到本机证书链问题，随后使用原生 Node 和系统信任库完成，未关闭 TLS 校验。漏洞库结果可能变化。

命令：`npm test`、`npm run build:demo`、`npm run check:package`。CI 在 Node 24 上重复测试、构建和安装包 TypeScript 接入检查；发布提交的结果见 [CI](https://github.com/SamuelSupe/slidekit/actions/workflows/ci.yml)。

## 浏览器证据

使用本机桌面 Chrome，没有引入新的浏览器自动化框架。通过本地 HTTP 服务验证演示页与构建后的 ESM／IIFE 示例。README 使用编辑器的实际截图。

此前已完成多页编辑、中文文字、局部格式、多选排版、缩放、放映、JSON 下载与恢复、PPTX 导入导出、双实例隔离、只读和销毁重挂。PDF 验证覆盖加密／取消导入，以及一个 171.72 MiB、12 页的合成图片 PDF；这是单次实测样例，不代表通用容量保证。

上一轮在构建后的 ESM 和 IIFE 接入页验证了隐藏页跳过、独立透明度、不可见线条、编号列表粘贴恢复与放映控制后的键盘焦点，并检查了实际下载的 JSON/PPTX。

### 本次发布检查 — 2026-09-25

- 构建后的 **ESM 与 IIFE**：注入同步／异步宿主保存异常，其他订阅和撤销重做仍正常；同 ID 文稿重载后拒绝陈旧图片插入。含控制字符的文字可导出并重新打开，保留合法中文与 emoji，原 JSON 不变。销毁重挂及第二个只读实例互不影响。
- 实际在**文字编辑中按 ⌘S**，下载 JSON 包含当前中文／英文草稿。重新打开文件恢复全部 3 个对象，第二实例保持 2 个对象、0 次变更。
- 通过实际文件选择器加载 JSON，并在受控的读取边界模拟宿主继续编辑：导入拒绝替换，保留新文字。延迟读取钩子仅位于被排除的 QA 页面。
- 最终静态 ESM／IIFE 检查无相关控制台错误或警告。首次检查曾命中开发服务器缓存的旧 `dist` 模块，改为静态发布服务后重新验证通过。
- 使用独立 XML 解析器检查实际下载的 PPTX，全部 XML 和关系文件可解析。
- tarball 在全新消费项目中离线安装，公开导出的严格 TypeScript 检查通过，覆盖隐藏页、渐变、透明度、导入和生命周期。包中包含双语文档、依赖许可与真实截图，排除 QA 样例。

地址：`http://127.0.0.1:5183/examples/esm.html`、`/examples/iife.html`，以及加载同一构建产物的合成 QA 页面。桌面视口为 1512×861；README 截图来自本机 Chrome 的 5182 端口演示页。

## Office 与未覆盖范围

- **当前版本未完成 WPS／PowerPoint 原生打开和编辑验收。**此前 WPS 操作被无法访问的原生对话框阻断；浏览器渲染与 OOXML 检查不能替代 Office 验收。
- 未穷尽验证其他桌面浏览器、手机触控、任意第三方 PPTX 或最大容量文稿。
- PDF 内容会图像化，PPTX 仅支持文档所列子集。字体度量、复杂效果、布局继承及不支持对象可能影响结果，见[兼容性说明](docs/guide.zh-CN.md#pptx-打开)。
- IndexedDB 仅用于演示便利，不等于备份或多标签协作机制；宿主需要处理持久化失败。

## 本地复核

```sh
npm ci
npm test
npm run build:demo
npm run check:package
npm run preview
```

在 Chrome 打开 `http://localhost:5183`、`/examples/esm.html` 和 `/examples/iife.html`。实际操作变更流程、检查控制台、保存并重新加载文稿，核对另一个实例不受影响。在宣称具体 Office 产品兼容前，需要手工打开和修改文件。请使用合成样例；个人验收文稿与临时产物不进入 Git 或发布包。
